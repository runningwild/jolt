# jolt

Jolt is a performance analysis tool designed to find the "knee" of the performance curve for block devices. Unlike traditional tools that just report metrics for fixed parameters, Jolt searches through parameter spaces (like queue depth or thread count) to identify optimal operating points.
